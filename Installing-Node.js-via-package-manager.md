# Add Chris Lea's repo:

```
sudo apt-get install python-software-properties
sudo apt-add-repository ppa:chris-lea/node.js
sudo apt-get update
```

# Then install:

```
sudo apt-get install nodejs
```